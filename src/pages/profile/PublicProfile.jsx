import React from 'react';
import { View, Text } from 'react-native';
import Layout from '@layout/layout';

const PublicProfile = () => {
    return (
        <Layout>
            <Text>상대방 프로필</Text>
        </Layout>
    );
};

export default PublicProfile; // 기본 내보내기
